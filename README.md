For local testing, execute for the first time only

    bundle install

to generate site locally (in _site):

    bundle exec jekyll build

to host site locally (auto-updates on changes):

    bundle exec jekyll serve
